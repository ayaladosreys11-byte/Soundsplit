# Base de dados PostgreSQL — copia a connection string do Railway/Neon/Supabase
DATABASE_URL="postgresql://utilizador:senha@host:5432/nome_da_bd"

# Chave secreta pra assinar as sessões (gera uma aleatória em produção)
JWT_SECRET="troca-isto-por-uma-string-aleatoria-longa"

# Stripe (usa as chaves de TEST do teu dashboard Stripe: dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY="sk_test_xxxxxxxxxxxx"
STRIPE_WEBHOOK_SECRET="whsec_xxxxxxxxxxxx"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# % que a plataforma retém das receitas de streaming antes de pagar o artista
PLATFORM_FEE_PERCENT="15"
